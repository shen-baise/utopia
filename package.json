{
  "name": "utopia",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal blog for shen-baise",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "check": "node scripts/check-build.mjs",
    "test": "playwright test"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@playwright/test": "^1.61.1"
  }
}
